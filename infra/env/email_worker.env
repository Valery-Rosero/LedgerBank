############################################
# Configuración SMTP para email_worker
# Por defecto usa MailHog (servicio "smtp" en Docker)
############################################

# === Desarrollo (MailHog) ===
SMTP_PROVIDER=gmail
SMTP_USER=nickyrosero159@gmail.com
SMTP_PASS=yvuk vnzc xnkf evvw
SMTP_FROM=vale.roserom23@gmail.com
SMTP_PORT=587
RABBITMQ_URL=amqp://rabbitmq:5672

# === Producción / Pruebas con Gmail ===
# Para activar Gmail, define estas variables:
# - habilita opcionalmente el provider
# Para usar MailHog en desarrollo, comenta las líneas de Gmail y usa:
# SMTP_HOST=smtp
# SMTP_PORT=1025
# SMTP_SECURE=false
#
# El worker detecta Gmail si hay credenciales válidas
# y usa host=smtp.gmail.com, port=465, secure=true automáticamente.